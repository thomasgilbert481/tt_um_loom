# Bug ledger

Every bug found by verification gets a row. Newest at the bottom.

| # | Date | Module | Symptom | Root cause | Found by (check ID) | Now covered by |
|---|---|---|---|---|---|---|
| 1 | 2026-09-15 | tt_um_loom (M0) | Verilator -Wall WIDTHEXPAND on comparisons against integer localparams | unsized compare | L0-LINT | zero-extended compare operands; lint in CI |
| 2 | 2026-09-17 | test/Makefile (template) | CI `gl_test` fails to compile: `Unknown module type: ihp_dff_r` in `sg13cmos5l_stdcell.v` | the cmos5l template Makefile omits the PDK primitives file `sg13cmos5l_udp.v`; design not involved | PHY-GL (first CI run) | UDP file added to the GL sources; `gl_test` runs on every push |
| 3 | 2026-09-18 | SEMANTICS 4 (spec), loom_timer, loomsim | `WAITB 3` could miss ticks indefinitely: at a tick period of 41 clocks a `WAITB 3; SETP` loop showed gaps of 84 | the rule cleared TICK_SEEN at every slot's commit edge, losing a tick that landed at edge x+1, between the slot's X cycle and its commit | RTL implementation review (docs/spec-questions/rtl-m2.md 4), confirmed by test_fifo against a literal model of the old rule | rule now clears only what the slot saw in X; RTL and golden model changed in one commit; test_fifo.test_waitb_tick_seen asserts no gap exceeds period + 4; co-simulation compares TICK_SEEN |
| 4 | 2026-09-19 | test_timing.py (test) | CI `gl_test` on the first macro hardening: `test_other_threads_do_not_move_the_edges` saw edge gaps [1, 63, ...] instead of 32; RTL passed | the test set RESET_PC1 = 0x40 but never reset thread 1, so the thread ran from its power-on PC: 0x40 in the 256-word M1 build (why it passed there), 0x80 in the 512-word build, unwritten memory. RTL decoded the X words harmlessly; the netlist spread X into the pin write mask and OUT0 read X one clock after every edge | PHY-GL (CI run 35419160398), reproduced locally with an RTL-vs-GL net diff | test resets thread 1 and checks its loop ran; `tb.v` stops an RTL run at the first valid slot decoding an X/Z instruction word (both builds) |
| 5 | 2026-09-19 | loom_pins, SEMANTICS 3/6.3, loomsim | a BIDIR pin in open-drain mode could drive high: `SETP pin, 1` while `OD_MASK` is 0, then `CSRW OD_MASK` to set it, then `OEP pin, 1` leaves the pad driving a one against whatever else is on the bus | the 6.3 write rules keep `PIN_OUT` at 0 under open drain, but nothing clears a 1 set before the mask was set, and `OEP` and the host's `PIN_OE` write are not qualified by `OD_MASK`; the pads were the plain register views | L4 formal PIN-1 (`formal/pins.sby`, finding F-2), reproduced on the golden model | the pads apply the mask (D-023); PIN-1, PIN-1CORE and PIN-1R proved; `test_pins.test_open_drain_never_drives_high` and `test_loomsim_pins.test_open_drain_set_after_a_pin_was_driving_high` |
| 6 | 2026-09-22 | loom_core (run control) | a host `STEP` write landing on the same edge at which the thread's F stage consumes an earlier `STEP_REQ` keeps `STEP_REQ` set (the host wins), where SEMANTICS 7 says the thread wins and the host's `STEP` is lost | the architectural-commit block clears `step_req_r[ph]` first and then assigns the whole vector from the host write, so the later assignment overrides the clear | review during the L4 ISO-1 work (the miter's author, reading the block; no check failed) | unreachable through the SPI port (a command takes far more than 4 clocks), so no test can produce it; RTL fixed the same day (the clear is now the last assignment) and SCHED-4 in `formal/sched.sby` proves the thread wins, with a cover point that reaches the coincidence |
