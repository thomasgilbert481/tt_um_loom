{
  "loom_core_L0357C035_const_a1f3": "under a documented constraint: infl feeds only thread_busy and from there dbg_running, the gate on host debug access. For a thread with RUN set, run_r already makes thread_busy 1, so infl matters only in the three clocks after RUN clears or a STEP retires while the slot drains D/X/W; a debug command needs several SPI bytes at SCK >= 8 core clocks (HOST_PROTOCOL), so no debug request can reach the core inside that window through the pads",
  "loom_core_L0360C009_cond_inv_390b": "under a documented constraint: infl feeds only thread_busy and from there dbg_running, the gate on host debug access. For a thread with RUN set, run_r already makes thread_busy 1, so infl matters only in the three clocks after RUN clears or a STEP retires while the slot drains D/X/W; a debug command needs several SPI bytes at SCK >= 8 core clocks (HOST_PROTOCOL), so no debug request can reach the core inside that window through the pads",
  "loom_core_L0360C010_stuck_f294": "under a documented constraint: infl feeds only thread_busy and from there dbg_running, the gate on host debug access. For a thread with RUN set, run_r already makes thread_busy 1, so infl matters only in the three clocks after RUN clears or a STEP retires while the slot drains D/X/W; a debug command needs several SPI bytes at SCK >= 8 core clocks (HOST_PROTOCOL), so no debug request can reach the core inside that window through the pads",
  "loom_core_L0361C032_bitop_4f2f": "under a documented constraint: infl feeds only thread_busy and from there dbg_running, the gate on host debug access. For a thread with RUN set, run_r already makes thread_busy 1, so infl matters only in the three clocks after RUN clears or a STEP retires while the slot drains D/X/W; a debug command needs several SPI bytes at SCK >= 8 core clocks (HOST_PROTOCOL), so no debug request can reach the core inside that window through the pads",
  "loom_core_L0862C032_const_7b9f": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0879C036_const_0f22": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0884C022_const_ee69": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0886C034_const_f365": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0886C052_const_0691": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0888C068_const_3016": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0892C021_const_5436": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L0892C042_const_140b": "by construction: the reset value of a pipeline payload register whose every consumer is qualified by a valid bit that itself resets to 0 (cw_pc/cw_aux from w_valid, w_sf_set masked by w_valid, op_a read only when vx), and which is reloaded unconditionally on every clock after reset",
  "loom_core_L1294C036_index_84b4": "by construction: the line lists bits in _unused_m2, a lint sink wire that nothing reads",
  "loom_fifo_L0031C031_const_8a1b": "dead default: loom_core always instantiates loom_fifo with #(.DEPTH(FIFO_DEPTH), .AW(FAW)) (loom_core.v:546, :554), so the parameter's default is never used",
  "loom_fifo_L0031C031_const_b606": "dead default: loom_core always instantiates loom_fifo with #(.DEPTH(FIFO_DEPTH), .AW(FAW)) (loom_core.v:546, :554), so the parameter's default is never used",
  "loom_fifo_L0054C034_const_e163": "same value at AW=2: {{(AW-2){1'b0}}, 1'b1} is {{0{1'b0}}, 1'b1} = 1'b1, which is the same increment of 1 as {{1{1'b0}}, 1'b1} = 2'b01",
  "loom_fifo_L0062C034_const_8614": "same value at AW=2: {{(AW-2){1'b0}}, 1'b1} is {{0{1'b0}}, 1'b1} = 1'b1, which is the same increment of 1 as {{1{1'b0}}, 1'b1} = 2'b01",
  "loom_pins_L0079C038_const_8114": "under a documented constraint: the pad synchroniser's reset value is replaced by the pads two clocks after reset, and no instruction can run that early because RUN resets to 0",
  "loom_pins_L0097C021_relop_d7b2": "by construction: the extra loop iteration indexes one past the end of the vector (bit 14 of the 14-bit PIN_OUT, bit 8 of the 8-bit PIN_OE); an out-of-range write does nothing in simulation and in synthesis, and the out-of-range read feeds only that write's condition",
  "loom_pins_L0097C023_const_44b0": "by construction: the extra loop iteration indexes one past the end of the vector (bit 14 of the 14-bit PIN_OUT, bit 8 of the 8-bit PIN_OE); an out-of-range write does nothing in simulation and in synthesis, and the out-of-range read feeds only that write's condition",
  "loom_pins_L0102C021_relop_df8d": "by construction: the extra loop iteration indexes one past the end of the vector (bit 14 of the 14-bit PIN_OUT, bit 8 of the 8-bit PIN_OE); an out-of-range write does nothing in simulation and in synthesis, and the out-of-range read feeds only that write's condition",
  "loom_pins_L0102C023_const_a5e0": "by construction: the extra loop iteration indexes one past the end of the vector (bit 14 of the 14-bit PIN_OUT, bit 8 of the 8-bit PIN_OE); an out-of-range write does nothing in simulation and in synthesis, and the out-of-range read feeds only that write's condition",
  "loom_spi_host_L0043C037_const_fd6b": "under a documented constraint: a synchroniser reset value is replaced by the pad two clocks after reset, and it can only matter if SCK moves inside that window with CS_n low, i.e. a host transaction in flight across a chip reset, which HOST_PROTOCOL rules out (formal finding F-3)",
  "loom_spi_host_L0045C037_const_c167": "under a documented constraint: a synchroniser reset value is replaced by the pad two clocks after reset, and it can only matter if SCK moves inside that window with CS_n low, i.e. a host transaction in flight across a chip reset, which HOST_PROTOCOL rules out (formal finding F-3)",
  "loom_spi_host_L0047C037_const_5e70": "under a documented constraint: a synchroniser reset value is replaced by the pad two clocks after reset, and it can only matter if SCK moves inside that window with CS_n low, i.e. a host transaction in flight across a chip reset, which HOST_PROTOCOL rules out (formal finding F-3)",
  "loom_spi_host_L0062C010_stuck_8a8f": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written (this mutant removes the reset branch altogether)",
  "loom_spi_host_L0063C023_const_593d": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0064C023_const_86a6": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0064C023_const_dce0": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0065C023_const_4e02": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0065C023_const_9921": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0066C023_const_0967": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0066C023_const_e218": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0067C023_const_4121": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0068C023_const_f5e6": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written; the one byte_done pulse it adds after reset is ignored because loom_host_ctl acts on byte_done only inside a transaction (cs_active)",
  "loom_spi_host_L0069C023_const_7ca9": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0069C023_const_fe40": "by construction: while CS_n is high loom_spi_host's idle branch rewrites bit_cnt, load_pending, rx_shift and tx_shift on every clock, byte_done is cleared on every clock and sck_q follows the pad on every clock, so a reset value only has to last until CS_n is first seen high, which it is at reset; rx_byte is read only at byte_done, after it has been written",
  "loom_spi_host_L0077C025_const_42bd": "under a documented constraint: an idle load_pending of 1 makes the first falling SCK edge of a transaction reload tx_shift instead of shifting it, which changes only MISO during the first byte, the command byte; HOST_PROTOCOL defines MISO only for the data words after the dummy byte",
  "loom_spi_host_L0078C025_const_aeb9": "by construction: rx_shift is seven bits and every byte shifts it seven times before rx_byte takes {rx_shift, mosi_s} at the eighth rising edge, so its idle value never reaches rx_byte",
  "loom_spi_host_L0078C025_const_d097": "by construction: rx_shift is seven bits and every byte shifts it seven times before rx_byte takes {rx_shift, mosi_s} at the eighth rising edge, so its idle value never reaches rx_byte",
  "loom_spi_host_L0095C041_const_e50d": "by construction: a bit shifted into tx_shift[0] needs eight shifts to reach tx_shift[7], but every byte reloads tx_shift after seven (load_pending, set at the eighth rising edge), so it never reaches MISO",
  "loom_timer_L0114C037_index_829b": "under a documented constraint: differs only at the far edge of reached()'s half window, for a deadline exactly 2^15 - 1 ticks behind NOW (SEMANTICS 4 and formal finding F-1 describe that wrap); firmware re-anchors with SETD long before a deadline goes that stale",
  "loom_timer_L0115C068_index_91d2": "under a documented constraint: differs only at the far edge of reached()'s half window, for a deadline exactly 2^15 - 1 ticks behind NOW (SEMANTICS 4 and formal finding F-1 describe that wrap); firmware re-anchors with SETD long before a deadline goes that stale",
  "loom_timer_L0125C024_const_30f7": "under a documented constraint: TICK_SEEN reads 1 instead of 0 right after reset, but TICK_INT resets to 1, so a tick lands on every edge and TICK_SEEN is 1 from the first edge after reset anyway; no instruction runs before then because RUN resets to 0"
}
